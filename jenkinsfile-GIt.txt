pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    echo 'Building the code using Maven...'
                    // Example: sh 'mvn clean package'
                }
            }
            
        }

        stage('Unit and Integration Tests') {
            steps {
                script {
                    echo 'Running unit and integration tests using JUnit...'
                    // Example: sh 'mvn test'
                }
            }
            post{
                success{
                    mail to: "adesolaadekunle71@gmail.com",
                    subject: "Unit and Integration Tests Status Email",
                    body: "Unit testing and integration using JUnit was successful!"
                }
            }
        }

        stage('Code Analysis') {
            steps {
                script {
                    echo 'Analyzing code using SonarQube...'
                    // Example: sh 'mvn sonar:sonar'
                }
            }
        }

        stage('Security Scan') {
            steps {
                script {
                    echo 'Performing security scan using OWASP ZAP...'
                    // Example: sh 'zap-cli start && zap-cli quick-scan http://localhost:8080'
                }
            }
            post{
                success{
                    mail to: "adesolaadekunle71@gmail.com",
                    subject: "Security Scan Email",
                    body: "Security scan was successful!"
                }
            }
        }

        stage('Deploy to Staging') {
            steps {
                script {
                    echo 'Deploying to staging server...'
                    // Example: sh 'scp target/myapp.jar user@staging-server:/path/to/deploy'
                }
            }
        }

        stage('Integration Tests on Staging') {
            steps {
                script {
                    echo 'Running integration tests on staging...'
                    // Example: sh 'mvn verify -Pstaging'
                }
            }
        }

        stage('Deploy to Production') {
            steps {
                script {
                    echo 'Deploying to production server...'
                    // Example: sh 'scp target/myapp.jar user@prod-server:/path/to/deploy'
                }
            }
        }
    }

    post {
        always {
            echo 'Sending notification emails...'
            // Example: emailext attachLog: true, subject: 'Build Notification', to: 'user@example.com'
        }
    }
}
